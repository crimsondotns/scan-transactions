Place the licensed font files here (not committed):

- SuisseIntl-Regular.woff2  → weight 400 (body)
- SuisseIntl-Book.woff2     → weight 500 (secondary / hint text)
- SuisseIntl-Medium.woff2   → weight 600 (labels, buttons, headings)

Preloaded from index.html, declared in src/styles/fonts.css. Numbers and hashes use this font too (no monospace).
