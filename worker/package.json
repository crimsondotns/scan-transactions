{
  "name": "xcap-api-wrapper",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Server-side API wrapper for XCap Scan — keeps the API secret off the static frontend",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
